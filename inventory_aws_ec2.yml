plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
keyed_groups:
  - key: tags.Name
    prefix: instance
hostnames:
  - dns-name
compose:
  ansible_host: public_ip_address
filters:
  instance-state-name: running
